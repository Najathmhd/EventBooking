@model EventBooking.Models.Venue

@{
    ViewData["Title"] = "Remove Venue";
}

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-lg-5 col-md-7">
            <div class="card shadow-lg border-0 animate-slide-up bg-white">
                <div class="card-body p-4 p-md-5 text-center">
                    <div class="bg-soft-danger d-inline-block p-4 rounded-circle mb-4">
                        <i class="fas fa-building-circle-exclamation text-danger fa-3x"></i>
                    </div>
                    <h2 class="fw-bold mb-3">Remove Venue?</h2>
                    <p class="text-muted mb-5">Thinking of removing <strong>@Model.Name</strong>? Make sure no upcoming events are scheduled here.</p>

                    <form asp-action="Delete">
                        <input type="hidden" asp-for="Id" />
                        <div class="d-grid gap-3">
                            <button type="submit" class="btn btn-danger btn-lg py-3 shadow-sm">
                                <i class="fas fa-trash me-2"></i>Remove Venue
                            </button>
                            <a asp-action="Index" class="btn btn-link text-muted text-decoration-none">
                                Take me back
                            </a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    .bg-soft-danger { background: rgba(239, 68, 68, 0.1); }
</style>
